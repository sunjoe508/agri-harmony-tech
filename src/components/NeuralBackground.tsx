// Decorative subtle neural-network grid + radial glow.
// Pure CSS via the .neural-bg class; mounts as a fixed background layer.
export function NeuralBackground() {
  return <div className="neural-bg" aria-hidden />;
}
